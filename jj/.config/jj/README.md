As per JJ docs (https://jj-vcs.github.io/jj/latest/config/#user-config-files)

this repo should be where JJ config is. However, I haven't managed jj to load it from there. Instead, `jj config path --user` points to `~/Library/Application Support`.
